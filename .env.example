# HorizonRIC environment configuration. Copy to `.env` and fill in.

# space-track.org credentials. Free account: https://www.space-track.org/auth/createAccount
# Used by horizon_ric.data.space_track.SpaceTrackClient and the
# `space_track` source connector.
SPACE_TRACK_IDENTITY=
SPACE_TRACK_PASSWORD=
