# Derived per-request rows are regenerated by build.py from pinned upstream
# objects. The 5GAD licence (MIT) would permit committing them; the repository
# discipline is to commit only manifest.json plus aggregate results.
generated/
__pycache__/
